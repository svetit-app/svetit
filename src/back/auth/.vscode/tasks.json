{
    "version": "2.0.0",
    "options": {
        "cwd": "${workspaceFolder}/../../../",
    },
    "tasks": [
        {
            "label": "build-task",
            "type": "shell",
            "command": "make build-auth",
        },
    ]
}