type: object
required:
  - list
  - total
properties:
  list:
    type: array
    maxItems: 100
    items:
      $ref: ./ControlCircuit.yaml
  total:
    type: integer
    format: int64
    minimum: 0
